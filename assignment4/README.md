# Angular

To run:

- Start api server:
    - Run: `python backend/apprunner.py`
- Start frontend server:
    - Go to app: `cd app`
    - Run: `ng serve`
- Open url `localhost:4200` in browser.
- Login:
    - Username: `user1`
    - Password: `pass`